git remote add origin url  -->> agrega un nuevo remoto al repositorioo del link ingresado
git remote remove origin --> desconecta el remoto
git remote -v >> muestra todos los remotes que hay en el repositorio

git push -u origin master >> hace un push desde mi rama "master" al remote "origin" del repositorio



git remote add origin
git push -u origin master


git push --tags  >> sube los tags a github
git push -f -> fuerza un push en github

git pull descarga todo los cambio hechos por los demas usuario al repositorio

git clone https://github.com/Aderly01/proyecto_implementacion.git emoney_rebuild>> clona un repositorio

git fetch >> git va actualizar localmente todos los cambios que se sucedieron en el repositorio de github
git push --set-upstream origin nombre-rama ->> hace push de una rama del repositorio local al repositorio en github

a
